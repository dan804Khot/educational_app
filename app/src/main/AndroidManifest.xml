<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EducationApp"
        tools:targetApi="31">
        <activity android:name=".RatingActivity" />
        <activity android:name=".ChooseAnswerActivity" />
        <activity android:name=".ChooseAnswerActivity2" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ChoiceOfDisciplinesActivity" />
        <activity android:name=".UserStatisticsActivity" />
        <activity android:name=".AboutMeActivity" />
        <activity android:name=".LevelMapActivity" />
        <activity android:name=".BadEndActivity" />
        <activity android:name=".GoodEndActivity" />
        <activity android:name=".CourseCompletedActivity" />
        <activity android:name=".RecoverFishActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EducationApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>